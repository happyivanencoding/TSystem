# 优化器

本目录已不再作为组合优化主线维护。

原 `optimiseur.ipynb` 和 `optimiseur.xlsm` 已移动到 `archive/frozen_20260629/优化器/`，只作为历史参考。当前唯一 Python 组合优化器主线是 [`../optimiser`](../optimiser)。

新代码不得从本目录读取 notebook/xlsm 或复制旧 Excel 流程；如需复用旧逻辑，应迁移到 `optimiser/` 或未来的 `tp_core.optimization`。
